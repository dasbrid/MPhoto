<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent">

    <Button android:id="@+id/btnStartSlideshow"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Slideshow"
            android:onClick="btnStartSlideshowClicked"
            android:layout_alignParentBottom="true"
            android:minWidth="200px" />
    <Button android:id="@+id/btnDelete"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Delete"
            android:onClick="btnDeleteClicked"
            android:layout_alignParentBottom="true"
            android:layout_toRightOf="@id/btnStartSlideshow"
            android:minWidth="200px" />
    <Button android:id="@+id/btnSelectAll"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Select All"
            android:onClick="btnSelectAllClicked"
            android:layout_alignParentBottom="true"
            android:layout_toRightOf="@id/btnDelete"
            android:minWidth="200px" />
    <Button android:id="@+id/btnSelectNone"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Select None"
            android:onClick="btnSelectNoneClicked"
            android:layout_alignParentBottom="true"
            android:layout_toRightOf="@id/btnSelectAll"
            android:minWidth="200px" />
    <Button android:id="@+id/btnShare"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Share"
            android:onClick="btnShareClicked"
            android:layout_alignParentBottom="true"
            android:layout_toRightOf="@id/btnSelectNone"
            android:minWidth="200px" />
    <GridView xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/grid_view"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:verticalSpacing="2dp"
              android:horizontalSpacing="2dp"
              android:background="#000111"
              android:columnWidth="@dimen/grid_column_width"
              android:numColumns="auto_fit"
              android:stretchMode="columnWidth"
              android:layout_above="@id/btnDelete"
    >
    </GridView>
</RelativeLayout>